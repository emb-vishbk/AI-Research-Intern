# Orchard classifier experiment

This fixture describes an image classifier that distinguishes apples from pears.
Its objective is to improve validation F1 while keeping prediction latency below
50 milliseconds. It contains no training code, data, credentials, or Azure job.

Verification marker: ORCHARD-READ-7319

This directory is only a read-only fixture for the first Copilot SDK session spike.
